{
 "additionalIdentifiers": [
  [
   "/properties/JobId"
  ]
 ],
 "additionalProperties": false,
 "createOnlyProperties": [
  "/properties/ProfileName"
 ],
 "definitions": {
  "S3SignedObject": {
   "additionalProperties": false,
   "description": "The Amazon S3 bucket name and key where Signer saved the signed code image.",
   "properties": {
    "BucketName": {
     "description": "Name of the S3 bucket.",
     "format": "AWS::S3::Bucket.Name",
     "type": "string"
    },
    "Key": {
     "description": "Key name that uniquely identifies a signed code image in the bucket.",
     "type": "string"
    }
   },
   "type": "object"
  },
  "S3Source": {
   "additionalProperties": false,
   "description": "Information about the Amazon S3 bucket where unsigned code is stored.",
   "properties": {
    "BucketName": {
     "description": "Name of the S3 bucket.",
     "format": "AWS::S3::Bucket.Name",
     "type": "string"
    },
    "Key": {
     "description": "Key name of the bucket object that contains unsigned code.",
     "type": "string"
    },
    "Version": {
     "description": "Version of the source image in the version-enabled S3 bucket.",
     "type": "string"
    }
   },
   "required": [
    "BucketName",
    "Key",
    "Version"
   ],
   "type": "object"
  },
  "SignedObject": {
   "additionalProperties": false,
   "description": "The S3 location of the signed code image.",
   "properties": {
    "S3": {
     "$ref": "#/definitions/S3SignedObject"
    }
   },
   "type": "object"
  },
  "Source": {
   "additionalProperties": false,
   "description": "Information about the S3 bucket where unsigned code is stored.",
   "properties": {
    "S3": {
     "$ref": "#/definitions/S3Source"
    }
   },
   "type": "object"
  }
 },
 "description": "Represents an AWS Signer signing job. Signing jobs are initiated via StartSigningJob and are read-only resources that provide visibility into code signing operations.",
 "handlers": {
  "list": {
   "permissions": [
    "signer:ListSigningJobs"
   ]
  },
  "read": {
   "permissions": [
    "signer:DescribeSigningJob"
   ]
  }
 },
 "primaryIdentifier": [
  "/properties/Arn"
 ],
 "properties": {
  "Arn": {
   "description": "The ARN of the signing job.",
   "type": "string"
  },
  "CompletedAt": {
   "description": "Date and time that the signing job was completed.",
   "format": "date-time",
   "type": "string"
  },
  "CreatedAt": {
   "description": "Date and time that the signing job was created.",
   "format": "date-time",
   "type": "string"
  },
  "JobId": {
   "description": "The ID of the signing job.",
   "type": "string"
  },
  "JobInvoker": {
   "description": "The IAM entity that initiated the signing job.",
   "maxLength": 12,
   "minLength": 12,
   "pattern": "^[0-9]{12}$",
   "type": "string"
  },
  "JobOwner": {
   "description": "The AWS account ID of the job owner.",
   "maxLength": 12,
   "minLength": 12,
   "pattern": "^[0-9]{12}$",
   "type": "string"
  },
  "PlatformDisplayName": {
   "description": "A human-readable name for the signing platform associated with the signing job.",
   "type": "string"
  },
  "PlatformId": {
   "description": "The microcontroller platform to which the signed code image will be distributed.",
   "type": "string"
  },
  "ProfileName": {
   "description": "The name of the signing profile.",
   "maxLength": 64,
   "minLength": 2,
   "pattern": "^[a-zA-Z0-9_]{2,}$",
   "type": "string"
  },
  "ProfileVersion": {
   "description": "The version of the signing profile used to initiate the signing job.",
   "maxLength": 10,
   "minLength": 10,
   "pattern": "^[a-zA-Z0-9]{10}$",
   "type": "string"
  },
  "RequestedBy": {
   "description": "The IAM principal that requested the signing job.",
   "type": "string"
  },
  "SignatureExpiresAt": {
   "description": "The expiration timestamp for the signature generated by the signing job.",
   "format": "date-time",
   "type": "string"
  },
  "SignedObject": {
   "$ref": "#/definitions/SignedObject",
   "description": "The S3 location of the signed code image."
  },
  "Source": {
   "$ref": "#/definitions/Source",
   "description": "The S3 bucket that contains the object to sign."
  },
  "Status": {
   "description": "Status of the signing job.",
   "enum": [
    "InProgress",
    "Failed",
    "Succeeded"
   ],
   "type": "string"
  }
 },
 "readOnlyProperties": [
  "/properties/Arn",
  "/properties/JobId",
  "/properties/Source",
  "/properties/Status",
  "/properties/CreatedAt",
  "/properties/CompletedAt",
  "/properties/SignatureExpiresAt",
  "/properties/RequestedBy",
  "/properties/JobOwner",
  "/properties/JobInvoker",
  "/properties/SignedObject",
  "/properties/PlatformId",
  "/properties/PlatformDisplayName",
  "/properties/ProfileVersion"
 ],
 "required": [
  "ProfileName"
 ],
 "tagging": {
  "taggable": false
 },
 "typeName": "AWS::Signer::SigningJob"
}
