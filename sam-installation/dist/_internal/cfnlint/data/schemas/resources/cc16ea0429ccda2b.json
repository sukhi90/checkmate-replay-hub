{
 "additionalIdentifiers": [
  [
   "/properties/WorkGroup",
   "/properties/SessionId"
  ]
 ],
 "additionalProperties": false,
 "createOnlyProperties": [
  "/properties/EngineConfiguration",
  "/properties/ExecutionRole",
  "/properties/WorkGroup"
 ],
 "definitions": {
  "EngineConfiguration": {
   "additionalProperties": false,
   "description": "Contains engine data processing unit (DPU) configuration settings.",
   "properties": {
    "AdditionalConfigs": {
     "additionalProperties": false,
     "description": "Contains additional notebook engine parameter mappings.",
     "patternProperties": {
      "^[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\t]*$": {
       "type": "string"
      }
     },
     "type": "object"
    },
    "CoordinatorDpuSize": {
     "description": "The number of DPUs to use for the coordinator.",
     "maximum": 1,
     "minimum": 1,
     "type": "integer"
    },
    "DefaultExecutorDpuSize": {
     "description": "The default number of DPUs to use for executors.",
     "maximum": 1,
     "minimum": 1,
     "type": "integer"
    },
    "MaxConcurrentDpus": {
     "description": "The maximum number of DPUs that can run concurrently.",
     "maximum": 5000,
     "minimum": 2,
     "type": "integer"
    },
    "SparkProperties": {
     "additionalProperties": false,
     "description": "Specifies custom jar files and Spark properties.",
     "patternProperties": {
      "^[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\t]*$": {
       "type": "string"
      }
     },
     "type": "object"
    }
   },
   "required": [
    "MaxConcurrentDpus"
   ],
   "type": "object"
  }
 },
 "description": "An Athena session is a Spark-based interactive environment for running calculations within a workgroup.",
 "handlers": {
  "list": {
   "handlerSchema": {
    "properties": {
     "WorkGroup": {
      "pattern": "^[a-zA-Z0-9._-]{1,128}$",
      "type": "string"
     }
    },
    "required": [
     "WorkGroup"
    ]
   },
   "permissions": [
    "athena:ListSessions"
   ]
  },
  "read": {
   "permissions": [
    "athena:GetSession"
   ]
  }
 },
 "primaryIdentifier": [
  "/properties/Arn"
 ],
 "properties": {
  "Arn": {
   "description": "The ARN of the session.",
   "type": "string"
  },
  "EngineConfiguration": {
   "$ref": "#/definitions/EngineConfiguration",
   "description": "Contains engine data processing unit (DPU) configuration settings."
  },
  "EngineVersion": {
   "description": "The engine version used by the session.",
   "maxLength": 128,
   "minLength": 1,
   "type": "string"
  },
  "ExecutionRole": {
   "description": "The ARN of the execution role used to access user resources for Spark sessions and Identity Center enabled workgroups.",
   "maxLength": 2048,
   "minLength": 20,
   "type": "string"
  },
  "SessionId": {
   "description": "The session ID.",
   "maxLength": 256,
   "minLength": 1,
   "type": "string"
  },
  "WorkGroup": {
   "description": "The workgroup to which the session belongs.",
   "pattern": "^[a-zA-Z0-9._-]{1,128}$",
   "type": "string"
  }
 },
 "readOnlyProperties": [
  "/properties/Arn",
  "/properties/SessionId",
  "/properties/EngineVersion",
  "/properties/EngineConfiguration/AdditionalConfigs",
  "/properties/EngineConfiguration/SparkProperties"
 ],
 "required": [
  "WorkGroup",
  "EngineConfiguration"
 ],
 "tagging": {
  "taggable": false
 },
 "typeName": "AWS::Athena::Session"
}
